@model IEnumerable<Entity.GetSentMessages_SP_Result>

<div class="inbox-header">
    <h1 class="pull-left">Sent</h1>
</div>
<div class="inbox-content">
    <table class="table table-striped table-advance table-hover">
        <thead>
            <tr>
                <th colspan="2">
                    <span>
                        To
                    </span>
                </th>
                <th colspan="3">
                    <span>
                        Subject
                    </span>
                </th>
                <th>
                    <span>
                        Date
                    </span>
                </th>
            </tr>
        </thead>
        <tbody>
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="6">You have not sent any messages...</td>
                </tr>
            }
            else
            {
                foreach (var message in Model)
                {
                    <tr class="message-sent" data-messageid="@message.Message_Id">
                        <td class="view-message hidden-xs">
                            @message.Reciever
                        </td>
                        <td class="view-message ">
                            @message.Subject
                        </td>
                        <td class="view-message text-right">
                            @message.Sent_Date
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
